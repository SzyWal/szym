function getAge(inputString){
  // return the girl's correct age as an integer. Happy coding :) 
  let a = inputString.slice('')
    return Math.floor(Math.random(a[0]) * (9 - 0) + 0)
    cosnole.log(a.slice(inputString))
  }